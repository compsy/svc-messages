import '../service'
const main = require('../service')
describe("main", function() {
  test('adds 1 + 2 to equal 3', () => {

jest.mock('./sound-player');
    console.log(service);
    expect(1 + 2).toBe(3);
  });
});
